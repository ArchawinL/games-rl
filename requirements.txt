# Kuhn-NFSP direct dependencies (high-level spec).
#
# Installed inside the Docker image with uv:
#   uv pip install --system -r requirements.txt
#
# Exact pins locked in Milestone 0 against python:3.11-slim / linux-amd64.
# `requirements.lock` records the full resolved set (direct + transitive).
# Backend decision: PyTorch NFSP (open_spiel.python.pytorch.nfsp) — TensorFlow
# fallback not needed.

# --- OpenSpiel + numerics ------------------------------------------------
open_spiel==2.0.2
numpy==1.26.4              # OpenSpiel 2.0.2 resolves against numpy 1.x
dm-tree==0.1.10           # required by open_spiel.python.pytorch.nfsp (imports `tree`)

# --- Deep RL: PyTorch NFSP backend ------------------------------------
# CPU-only wheel; the '+cpu' local tag is served only from the index below.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu

# --- Config / data / plotting ---------------------------------------
pyyaml==6.0.3
pandas==3.0.5
matplotlib==3.11.1
tqdm==4.66.5

# --- Tests ------------------------------------------------------------
pytest==9.1.1
